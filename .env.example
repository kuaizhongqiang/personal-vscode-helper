# Personal VSCode Helper — 环境变量

## VSCode 插件配置
# 在 VSCode 设置中配置，参见 package.json contributes.configuration

## 服务端配置（services/personal-helper-server/）
# 复制 services/personal-helper-server/.env.example 到 .env 后修改

# 服务端口
PORT=3000

# 数据存储目录（默认 ~/.helper）
HELPER_DATA_DIR=./data

# API 鉴权 Token（空则不启用）
API_TOKEN=
