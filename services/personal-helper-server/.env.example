# Personal Helper Server — 环境变量配置

# 服务端口（默认 3000）
PORT=3000

# 数据存储目录（默认 ~/.helper）
HELPER_DATA_DIR=/path/to/data/dir

# API Token（必填，否则会自动生成）
# 所有 REST 请求（除 /api/health 外）需要携带：
#   Authorization: Bearer <token>
API_TOKEN=your-secure-token-here
