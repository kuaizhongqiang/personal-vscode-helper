# Personal Helper Server — 环境变量配置

# 服务端口（默认 3000）
PORT=3000

# 数据存储目录（默认 ~/.helper）
HELPER_DATA_DIR=/path/to/data/dir

# API Token（为空则不启用认证）
# 如果设置，所有 REST 请求需要 Authorization: Bearer <token>
API_TOKEN=
